<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bibo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/bibo_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bibo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ShowCheck"></activity>
        <activity android:name=".ExportSuccess"></activity>
        <activity android:name=".ImportSuccess"></activity> <!-- <activity android:name=".Menu"/> -->
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity android:name=".SignUp" /> -->
        <!--  -->
        <!-- <activity android:name=".Login"/> -->
        <activity android:name=".Import" />
        <activity android:name=".Check" />
        <activity android:name=".Delete" />
        <activity android:name=".Adding" />
        <activity android:name=".AddMore" />
        <activity android:name=".Export" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>